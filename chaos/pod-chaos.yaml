apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-servico
  namespace: trabalho-sis-dis
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - trabalho-sis-dis
    labelSelectors:
      app: servico
  duration: "10s"
